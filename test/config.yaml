# ======================
# File: config.yaml
# ======================

# Address of your DNS gRPC server
serverAddress: "172.20.0.2:30808"

# Default DNS entry parameters for testing.
dns:
  podName: "pingo"
  ipAddress: "10.0.1.2"
  network: "test-network"
  scope: "inter"
